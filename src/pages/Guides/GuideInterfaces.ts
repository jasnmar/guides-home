

export interface guideDataInterface {
  id: number,
  firstName: string,
  lastName?: string,
  started?: string,
  level: string,
  location: string,
  bio: string[],
  headshot?: string,
  venmoImage?: string,
  venmoLink?: string,
}